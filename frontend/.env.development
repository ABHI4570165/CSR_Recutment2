VITE_API_URL=http://localhost:8080
# Live proctoring — local backend has the signaling server. Activates the /live page
# + student camera broadcast in local dev.
VITE_SIGNALING_URL=http://localhost:8080
